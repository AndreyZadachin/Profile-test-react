import './OvalInput.css';

const OvalInput = () => {
  return (
    <label className="switch">
      <input type="checkbox" />
      <span className="slider "></span>
    </label>
  );
};

export default OvalInput;
